# ORDER F-3.6b — Delete-not-harden cleanup · Email Followupper
Workspace: Email-Folllowuper · Branch: claude/f-36b-delete-not-harden · Repo: MichaelMobupps/doctrine-followup-export

## Context (fixed — do not re-litigate)
- F-D4 (2026-08-09) and F-3.6a recorded the items below as debt to DELETE, not harden. F-3.6a is live in production: three-state connection health, evidence-preserving 2-strike retries, due-query excludes paused and auth-dead, stranded recovery, /api/admin/cron-heartbeats.
- Dev and production schemas were aligned 2026-08-09; a full diff returns empty except uq_followups_prospect_cycle_stage (deliberate column-order drift, skipped by name in startupMigrations.ts — NEVER touch it).
- Landmine 1: a null-user (ownerless) prospect bypasses the per-user credentials guard and falls through to the env fallback (GOOGLE_REFRESH_TOKEN + SENDER_EMAIL), so an ownerless prospect coming due would send from the fallback identity instead of refusing. Found live by F-3.6a's smoke.
- Landmine 2: the legacy zero-connected-users sync branch (gmailSync.ts fallback path, ~:855 in the July analysis) swallows failure and reports ok with synced:0 — it masked total death for months (D2, July 31).
- Bug: queueStageForProspect ignores cycle and can strand an AntiGhosting cycle-2 stage.
- Schema ownership gap: cron_heartbeats is created by drizzle-kit push, not by startupMigrations.ts, so a from-scratch production database would boot without it.
- Stale declaration: idx_prospects_app is declared at lib/db/src/schema/prospects.ts:157, exists in neither database, and nothing creates it.
- Schema mechanism of this repo: NO drizzle journal anywhere; artifacts/api-server/src/lib/startupMigrations.ts (idempotent, runs at every api-server boot) is the only lawful path to production schema. Replit's publish flow diffs production against the dev database and proposes DROPs for anything dev lacks.

## Scope
1. Delete the ownerless-prospect env fallback in the send/generation path: a prospect with no owning user REFUSES to send, records an honest failure_reason (owner_missing), and never touches GOOGLE_REFRESH_TOKEN or SENDER_EMAIL. Find every read of those variables on the send path and remove the fallback entirely; OAuth machinery that legitimately uses the client secrets stays.
2. Delete the legacy zero-users sync branch. With zero connected accounts the sync reports its true condition rather than ok/synced:0; code reachable only through that branch goes with it.
3. Fix queueStageForProspect to respect cycle so an AntiGhosting cycle-2 stage queues correctly; add a regression test that fails on the old behavior.
4. Move cron_heartbeats creation into startupMigrations.ts as an idempotent statement matching the live production shape byte-for-byte, same guard pattern as the F-3.6a statements. drizzle-kit push must not be required for a fresh boot.
5. idx_prospects_app: report whether any query filters or sorts on that column. If unused, delete the declaration from the schema. If used, add its creation to startupMigrations.ts exactly as declared. Either way, dev and production must end identical in the publish diff's eyes.
6. Dev-database alignment (standing law — any order shipping schema changes includes this): after the changes, run runStartupMigrations() against the dev database via the guarded tsx pattern from 2026-08-09 (URL must contain helium and must not equal PROD_DATABASE_URL; no server boot, no cron, no sync, no sends). Then state exactly what the regenerated publish plan should show before Michael touches Publish. Expected: no schema changes, or exactly the one new index if scope 5 chose "add".

## Out of scope
- uq_followups_prospect_cycle_stage — never touch it.
- Any send-window, cap, model, or provider change.
- The Chief seam (its own orders, F-3.7a and C-3.7b).
- Any write to the production database. Michael publishes; production migrates itself at boot.

## Ritual specifics
- Blast-radius statement first: files, behaviors, worst failure, rollback point (push tag pre-f-36b-main-tip before merging).
- Gates: typecheck, full test suite, build.
- Godlike audit rounds: technical, security, end-user framings.
- Smoke dark and lit on isolated ephemeral databases with vendors made impossible; facts read from the server's own logs. The smoke MUST include: an ownerless prospect coming due refuses with owner_missing and no send attempt; zero-users sync reports its true condition; a cycle-2 AntiGhosting stage queues; a from-scratch ephemeral database boots to the full schema including cron_heartbeats via startupMigrations alone.
- Mutation proof: reintroduce each deleted branch and defect briefly and watch tests bite — at minimum restore the env fallback, restore the cycle-ignoring queue, and drop cron_heartbeats from the migration.
- Ledger entry in TODO.md, then merge to main and push (the repo-local credential fix is recorded in TODO.md if the push is refused).

## Hard rules
- No destructive git, no force-push. No publishing — Michael publishes. No secret changes — Michael owns secrets. Separate ports; never touch the running workflow. Vendor calls forbidden in tests and smoke. NEVER send email — the smoke proves refusal, not delivery. Halt after three failed attempts on one cause and ask. Halt on any broken premise — premise corrections are welcomed.
