# ORDER C-3.7b — The Followupper on the deck: rendering, health panel, escalation · The Chief
Workspace: Chief-Ship · Repo: github.com/MichaelMobupps/Chief-Ship · Branch: claude/c-37b-followup-deck

## Context (fixed — do not re-litigate)
- F-3.7a is live on the Followupper and the uplink pair is set; the card probes successfully. The authoritative contract is §9 of the F-3.7a report; Michael uploads that report to this repo root as F-3.7a_REPORT-appside.md — read its §9 in full before designing. The load-bearing clauses:
  - Two paths only: GET /api/chief/status and GET /api/chief/accounts at FOLLOWUP_URL (custom domain). Auth is Bearer FOLLOWUP_TOKEN, constant-time; EVERY failure — including an unknown path or method under /api/chief with a VALID token — answers one byte-identical 401. This is a deliberate departure from the Chief's own §5: a 404 never confirms a token here, and a path typo reads as "token rejected". Probe only the two written paths.
  - accepting_jobs and active_jobs are DELIBERATELY ABSENT, in every state. The contract directs the Chief to render, for followup only: "n/a — this app takes no jobs" for both — NOT a bare dash, which reads as a failed answer. The real signal is health.
  - health carries: accounts census {connected, auth_dead, paused, disconnected, total} counted by the same function that labels /accounts; due_queue_depth (the scheduler's own WHERE, held accounts excluded); sending_paused_globally; oldest_heartbeat_age_seconds (the WORST per-cron age — the liveness number; but daily/weekly ticks make the single figure spiky, so alerting keys on per-tick ages in health.crons[], each with age_seconds, ticks_24h, errors_24h); budget with BOTH day windows (UTC for spend_today_usd, Asia/Jerusalem for the app's own cap; they differ by up to three hours).
  - A 503 {"error":"status unavailable"} means the app cannot read its own state and refused to fabricate zeros; treat as ask-again-later, never as an all-healthy zero census.
  - /accounts: paged (limit 1..200 default 50, garbage falls back, no 400), per-account {id, label (never an address), state connected|disconnected|auth_dead|paused with auth_dead outranking paused, paused_by_admin, auth_dead_since, auth_dead_reason, last_send_at, queue_depth}.
- Why this order exists: six auth-dead grants once sat unnoticed for ten days. The uplink can now see them; this order makes the Chief say so, loudly, the morning it happens.

## Scope
1. Card rendering per contract: the followup card shows "n/a — this app takes no jobs" for the two absent fields; the health line joins the card (census summary, due-queue depth, worst heartbeat age); a 503 or unreachable probe renders an honest ask-again-later state, never zeros; spend today continues from the same probe. The other three apps' cards are byte-identical to today — pinned by test.
2. Control Center panel "Followup accounts": the /accounts page rendered as a table — label, state chip (auth_dead red and outranking paused per contract precedence), paused flag, auth_dead_since and reason, last send, queue depth — paged with next_offset, refresh button, probed through the existing appClient discipline (no redirects, 64 KB, timeout, origins re-checked).
3. Auth-dead escalation with state memory: the Chief remembers each followup account's last known state (new table via the checksummed migration runner). On a probe showing an account transitioning INTO auth_dead: one WARN ledger line naming the account label and reason, and Pushover when keys exist — once per transition, never per probe. Recovery (auth_dead → connected) writes one INFO line. Additionally: sending_paused_globally turning true, and the followup status being 503/unreachable continuously for more than 15 minutes, each earn one WARN with the same once-per-transition discipline.
4. Cron staleness: a per-tick WARN when a tick's age exceeds a sane multiple of its cadence (the contract lists each tick's schedule); keyed per tick name, once per transition into staleness. The single oldest_heartbeat figure is displayed but never alerted on alone.
5. Spend visibility: assert in tests that followup's ingested spend rows render in the all-reporters tile and ledger exactly like the other apps' (the seam already works; this pins it).
6. Migration under the standing laws: dev alignment through the app's runner before Michael touches Publish; check-prod-migration-state.sql and stamp-prod-migrations.sql regenerated and proven; expected publish plan stated (CREATE-only for the state table, or exactly what exists).

## Out of scope
- Any command, write, enrol, pause, or send toward the Followupper — capabilities.chief_writes is false and stays false.
- Reply intelligence. The Chat app. Any Followupper-side change.

## Ritual specifics
- Blast-radius statement first; rollback tag pre-c-37b-main-tip.
- Gates: npm run build green before merge and publish.
- Godlike audit rounds: technical, security, end-user (the card must read honestly to a tired operator at a glance).
- Smoke dark and lit on ephemeral databases, vendors impossible: fixtures for a healthy census, an auth-dead transition (WARN fired once across three consecutive probes), a recovery (INFO once), a 503 rendered as ask-again-later, a stale fast_tick WARN, pagination across a multi-page accounts fixture, and the three untouched cards byte-identical.
- Mutation proof: make the escalation fire per-probe and watch a test bite; render zeros on a 503 and watch a test bite; render a bare dash for accepting_jobs on followup and watch a test bite; probe a third path under /api/chief and watch the two-paths-only pin bite.
- Ledger entry in TODO.md; merge to main and push.

## Hard rules
- No destructive git, no force-push. No publishing — Michael publishes; dev alignment and stamp/check regeneration first, expected plan stated. No secret changes. Vendor calls forbidden in tests and smoke. NEVER send email. Halt after three failed attempts on one cause and ask. Halt on any broken premise — premise corrections are welcomed.
