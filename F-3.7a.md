# ORDER F-3.7a — The Chief's uplink: machine surface on the Followupper
Workspace: Email-Folllowuper · Repo: MichaelMobupps/doctrine-followup-export · Branch: claude/f-37a-chief-uplink

## Context (fixed — do not re-litigate)
- The Chief's FLIGHT deck probes each app through FOLLOWUP_URL + FOLLOWUP_TOKEN (provisioned on the Chief, unset today — the card honestly reads NO LINK). This order gives the Followupper the surface that link expects. v1 capabilities per Michael (2026-08-11): HEALTH PULSE (read-only) and SPEND REPORTING (outbound). Enrollment or any Chief-commanded write is deliberately NOT in v1; reply intelligence comes later.
- Fleet HTTP contract style, proven on Leadfinder and Prospector: Bearer order-token, case-sensitive scheme, constant-time comparison, exactly one indistinguishable 401 body for every auth failure, no-store, JSON envelope, pages under 64 KB, idempotency by external_id wherever a write exists. Read the Chief's CONTRACT.md and the Prospector's chief endpoints in this fleet for the exact card expectations before designing; the Chief's appClient refuses redirects and re-checks origins.
- Why this seam exists: six auth-dead Gmail grants sat unnoticed for ~10 days in early August. The Chief watching this app's health ends that class of silent death.
- This repo's schema mechanism is startupMigrations.ts only (no drizzle journal); any new table ships as an idempotent startup migration, dev gets booted before Michael ever touches Publish, and the expected publish plan is stated in the report.

## Scope
1. GET <prefix>/api/chief/status, order-token gated: app identity and version, server time, and the numbers the uplink card renders — spend today, plus this app's honest equivalents of the card fields (accepting_jobs semantics for an app that takes no jobs: report capabilities truthfully rather than faking the Prospector's shape; state in the contract exactly what the Chief should render). Include the health summary: connected / auth_dead / paused account counts, due-queue depth, and the oldest heartbeat age per cron from cron_heartbeats.
2. GET <prefix>/api/chief/accounts, order-token gated, paged under 64 KB: per account — display label (never an email address on the wire; F-3.6b-era identity discipline holds), connection state (connected | auth_dead | paused), last send time, queue depth. This is the page that would have surfaced the ten silent days.
3. Spend reporting outbound: report this app's own LLM spend to the Chief's POST /api/ingest/spend on the Prospector's proven pattern — per UTC day per vendor in $0.50 quanta, one request per quantum, external_id idempotent, initiated_by human, retry 5xx with the same external_id, never retry 4xx, 401 = stop and alert. Config: CHIEF_URL + CHIEF_INGEST_TOKEN; unset means the reporter is dormant and says so loudly once per boot, never crashes.
4. Token: introduce the inbound order-token secret (name it clearly, e.g. FOLLOWUP_CHIEF_TOKEN); the report tells Michael the exact pair to set — this secret here, and FOLLOWUP_URL (the .replit.app address with this app's real API prefix, stated verbatim) + FOLLOWUP_TOKEN on the Chief.
5. If a spend-cursor table is needed, it ships as an idempotent startup migration; dev boot before publish; expected publish plan stated (CREATE-only or empty).
6. Print the as-built contract verbatim at the end of the report — endpoints, envelope, field semantics, auth behavior, page discipline — ready to embed in the Chief-side order C-3.7b.

## Out of scope
- Any endpoint that lets the Chief write, enroll, pause, or command anything in this app.
- Reply/sentiment data over the seam (later order).
- The Chat app entirely.
- Any send behavior, any Gmail write, any draft.

## Ritual specifics
- Blast-radius statement first; rollback tag pre-f-37a-main-tip.
- Gates: typecheck, full test suite, build.
- Godlike audit rounds: technical, security (401 indistinguishability timing; no email addresses in any payload — grep the whole HTTP surface in smoke), end-user.
- Smoke dark and lit on isolated ephemeral databases, vendors impossible, NEVER sending email: status and accounts render truthful fixtures including an auth-dead and a paused account; pagination at the 64 KB boundary; auth failures byte-identical for wrong token, malformed header, and valid-token-wrong-path; the spend reporter emits correct quanta against a fake Chief, is idempotent on replay, and is dormant-and-loud with config unset.
- Mutation proof: make the 401s distinguishable and watch a test bite; double-report a quantum and watch idempotency bite; let an email address into an accounts payload and watch the leak grep bite.
- Ledger entry in TODO.md; merge to main and push (the repo-local gh credential fix is the standing push path).

## Hard rules
- No destructive git, no force-push. No publishing — Michael publishes; dev boots before Publish per the standing law. No secret changes — the report names the secrets, Michael sets them. Vendor calls forbidden in tests and smoke. NEVER send email. Halt after three failed attempts on one cause and ask. Halt on any broken premise — premise corrections are welcomed.
