# F-3.7b — The tick stops lying and stops stalling: skip heartbeats, wedge watchdog, request timeouts, per-row deadline

**Order id:** F-3.7b · **Workspace:** Email-Folllowuper · **Repo:** doctrine-followup-export · **Branch:** claude/f-37b-honest-tick

---

## Context (fixed — do not relitigate)

The all-day followup_cron_stale alarms are a false death report with a real disease
underneath, per the August 13 read-only diagnosis. False report: fast_tick returns
bare on the contention guard and writes NO heartbeat, so the machine liveness
signal (max fired_at per tick) starves whenever passes run long, and the Chief's
age line lies. Real disease: a 20-row pass carries 2s hard sleep per row, up to
three Anthropic call sites at worst ~5.25 min each (~16 min for ONE poisoned row),
and Gmail calls with NO request timeout at all (the known TODO gap) — normal
passes run 15–25 minutes and one overload episode pushes past 40. Masked worse
failure: processTickRunning has no wedge watchdog and no pass-identity token, so
one hung socket would freeze all sending forever while process_due keeps saying
ok. Michael's ruling: fix it properly — all four steps of the diagnosis, in its
order. DUE_BATCH_LIMIT stays at 20 (the throughput trade is not taken).

Standing repo laws bind: schema only via startupMigrations.ts (none expected
here); never touch the drizzle-push baseline; never approve any plan containing
DROP INDEX idx_cron_heartbeats_tick_fired_at; tag pre-wipe-2026-07-29 never
pushed; push via the recorded gh repo-local helper.

## Scope

1. **Record the skip.** fast_tick writes a heartbeat on the guarded path exactly
   as process_due does (outcome ok, details.skipped) — the age stops lying, the
   Chief's staleness rule works as written with no Chief-side change. The old
   "20k+ skipped rows would drown a human" comment is updated to say why skips
   are recorded now (the column became a machine liveness signal in F-3.7a).

2. **Bound the guard.** processTickRunning gains a wedge watchdog and
   pass-identity token, mirroring the sync guard (gmailSync.ts:845–867) but sized
   for this tick — minutes, not the sync path's 4 hours. A wedged pass is broken
   loudly, ledgered, and the next pass runs. State the chosen limit and why.

3. **googleapis request timeouts.** Close the recorded TODO gap: every googleapis
   call carries a request timeout; state the value(s). Until this exists no
   watchdog can honestly say what it is reclaiming.

4. **Per-row generation wall-clock deadline.** A row whose generation exceeds its
   deadline fails THAT row with preserved evidence under the F-3.6a retry policy
   and the pass moves on — one poisoned row can no longer eat 16 minutes of a
   3-minute tick's budget. State the deadline and its relation to the per-call
   caps.

## Out of scope

DUE_BATCH_LIMIT and any throughput tuning. Schema changes. Chief-side anything.
The 18-statement drizzle-push baseline (its schema-ownership decision stays its
own order). Send behavior, windows, caps.

## Ritual specifics

Blast radius first. Gates. Audit rounds. Smoke dark and lit on ephemeral state
with vendors impossible — **prove at minimum: a guarded fast_tick writes the skip
heartbeat and max(fired_at) advances; a wedged flag is reclaimed within the
watchdog limit and sending resumes; a googleapis call that never answers is cut
at the timeout; a row exceeding its deadline fails alone with evidence while the
rest of the pass completes; heartbeat volume at the 3-minute cadence stays within
whatever prune/retention already governs the table.** Mutation proofs (at
minimum: remove the skip heartbeat and prove the liveness test bites; remove the
watchdog and prove the wedge test bites; remove the per-row deadline and prove
the poisoned-row test bites). Ledger. Merge to main and push (tag
pre-f-37b-main-tip first).

## Hard rules

No destructive git. No publishing — Michael publishes. No secret changes. Vendor
calls forbidden in tests and smoke. NEVER send a real email from any test or
smoke. Halt after three failed attempts on one cause. Halt on broken premises —
corrections welcomed.
